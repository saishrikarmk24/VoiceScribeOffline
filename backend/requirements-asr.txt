# Optional GPU diarization only. Do NOT install on a 6 GB laptop.
# This pulls PyTorch + CUDA and is the usual "CUDA problem" on a friend's PC.
# Default VoiceScribe uses CPU Faster-Whisper from requirements.txt instead.
#
#   pip install pyannote.audio
